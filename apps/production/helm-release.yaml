apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: master-laravel
  namespace: production
spec:
  chartRef:
    kind: OCIRepository
    name: master-laravel-oci
    namespace: production
  interval: 10m
  values:
    nginx:
      ingress:
        enabled: false